{
  "rules": {
    "users": {
      "$uid": {
        "2023": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "tags": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}
